

const swordPower = 50;
const hasShield = false;
const armor = "Gothic-style armour"
const name = "Sir Debugalot"

console.log(`
    ${name} Is Ready For Battle,
    Width Sword Power of ${swordPower}
    With Armor Type ${armor}
    ${hasShield ? "And With Shield" : "And Without Shield"}
    `)
